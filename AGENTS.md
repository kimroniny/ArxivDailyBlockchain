# AGENTS.md

## Cursor Cloud specific instructions

This repository (`ArxivDailyBlockchain`) is currently a bare/empty project with only a `README.md`. It is intended to aggregate blockchain-related papers from arXiv, but no application code has been implemented yet.

- **No build/lint/test commands exist** — there are no source files, dependency manifests, or test suites.
- **No services to run** — there are no Docker, Node, Python, or other runtime configurations.
- **No dependencies to install** — there are no `package.json`, `requirements.txt`, `Cargo.toml`, or similar files.

Once application code is added, this file should be updated with relevant setup, build, lint, test, and run instructions.
